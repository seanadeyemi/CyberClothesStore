@model IEnumerable<Store.Domain.ICategory>

<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">Shop <b class="caret"></b></a>
@*@Html.ActionLink("Shop", "Index", "Home", null, new { @class = "dropdown-toggle", @toggle = "dropdown"})*@
<ul class="dropdown-menu">
    
    @foreach (var cat in Model)
    {
        <li>
            
            @Html.ActionLink(cat.Name, "ClothesByCategory", "Home", new { name = @cat.Name}, new { @class = "" })
        </li>
    }


</ul>

